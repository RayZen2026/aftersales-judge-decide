# P0规则修复方案

## 一、问题发现

### 1.1 数据分析结果

已处理的超时升级售后单8条，分析如下：

| 订单号 | 间隔天数 | 门店等级 | 严重品质 | 判责结果 | P0-1触发 |
|--------|---------|---------|---------|---------|----------|
| UAS127010205606105134 | 13天 | B | 0 | 10:90 | ❌ |
| UAS127197398219890712 | 5天 | A | 0 | 20:80 | ❌ |
| UAS126699717835902985 | 8天 | B | 0 | 10:90 | ❌ |
| UAS126696825552916576 | 8天 | A | 0 | 10:90 | ❌ |
| 其他4条 | <10天 | A/B | 1 | 10:90 | ❌ |

**关键发现**：
- ✅ 8条超时单中，1条满足P0-1的前4个条件（间隔≥11天、门店B、非严重品质）
- ❌ 但该订单的商家偏离倍数>0.3，导致P0-1不触发
- ❌ **所有超时单都未触发P0-1规则**

### 1.2 问题1：P0-4小额规则缺少超时单特殊处理

**现状**：
```
P0-4触发后：
- 若 merchant > 0 → merchant=100%
- 若 merchant=0 且 platform>0 → platform=100%
```

**问题**：
- 处理中升级售后单：商家问题 → 商家100% ✅ 符合预期
- **超时升级售后单：应该平台100%，但当前逻辑会按merchant是否>0判断** ❌

**案例**（假设）：
- 超时单 + 小额触发 + merchant>0 → 当前输出merchant=100%
- **期望**：超时单 + 小额触发 → 应该platform=100%（不论merchant是否>0）

### 1.3 问题2：P0-1超时规则条件5过严

**现状**：
```
P0-1触发条件5：商家偏离倍数 = 0 或 < 0.3
```

**问题**：
- UAS127010205606105134：间隔13天、B级门店、赔付率4.78%、非严重品质
- 但商家偏离倍数1.95 → P0-1不触发
- **实际超时13天的门店，应该由平台承担主要责任（时效问题）**

**业务逻辑分析**：
- P0-1的目的：超时单由平台承担（时效责任）
- 条件5的目的：排除"商家明显有问题"的情况
- 当前阈值0.3过严：商家偏离倍数1.0-2.0的门店，可能只是品质略差，不应该完全否定时效责任

## 二、修改方案

### 2.1 修改1：P0-4增加超时单特殊逻辑（高优先级）

**修改位置**：`assets/agent_single_prompt_template.j2` 行293-301

**修改前**：
```jinja
**P0-4: 小额规则（最终兜底方案）**
触发条件（需**同时满足**2条）：
1. 诉求赔付金额 / 商品实付金额 < 10%
2. 诉求赔付金额 ≤ 30元

触发后果（**计算完所有调整后**，作为**最终兜底方案**强制修正）：
- 若存在商家责任（merchant > 0）→ responsibility: merchant=100%, platform=0%, logistics=0%, agent=0%
- 若无商家责任但有平台责任（merchant=0 且 platform > 0）→ responsibility: platform=100%, merchant=0%, logistics=0%, agent=0%
- 判责依据必须写明："小额申诉（诉求{X}元，占实付{Y}%），触发P0-4兜底规则，{商家/平台}承担100%全部责任"
```

**修改后**：
```jinja
**P0-4: 小额规则（最终兜底方案）**
触发条件（需**同时满足**2条）：
1. 诉求赔付金额 / 商品实付金额 < 10%
2. 诉求赔付金额 ≤ 30元

触发后果（**计算完所有调整后**，作为**最终兜底方案**强制修正）：
- **若升级售后类型="超时升级售后单"** → responsibility: platform=100%, merchant=0%, logistics=0%, agent=0%（超时单小额申诉由平台承担）
- 否则：
  - 若存在商家责任（merchant > 0）→ responsibility: merchant=100%, platform=0%, logistics=0%, agent=0%
  - 若无商家责任但有平台责任（merchant=0 且 platform > 0）→ responsibility: platform=100%, merchant=0%, logistics=0%, agent=0%
- 判责依据必须写明："小额申诉（诉求{X}元，占实付{Y}%），触发P0-4兜底规则，{超时单平台承担/商家承担/平台承担}100%全部责任"
```

**同步修改**：
- 行562-573：步骤6的P0-4最终兜底描述（增加超时单判断）
- 行1084：任务步骤7的P0-4检查逻辑（确保LLM检查升级售后类型）

### 2.2 修改2：P0-1条件5放宽（建议评估后决定）

**方案A：放宽到1.0**（推荐）
```jinja
5. 商家偏离倍数 = 0 或 < 1.0（无明显商家问题）
```

**影响**：
- UAS127010205606105134（偏离1.95）：仍不满足
- 但偏离0.3-1.0的超时单可以触发P0-1

**方案B：放宽到2.0**（激进）
```jinja
5. 商家偏离倍数 = 0 或 < 2.0（商家问题不严重）
```

**影响**：
- UAS127010205606105134（偏离1.95）：满足 ✅
- 但偏离1.0-2.0的商家可能确实有品质问题

**方案C：删除条件5**（不推荐）
- 影响：所有超时单只要满足前4条就触发P0-1
- 风险：即使商家明显有问题，也由平台承担

**建议**：采用方案A（放宽到1.0），观察效果后再决定是否进一步放宽

### 2.3 修改3：P0-4计算示例优化（降低LLM计算错误）

**问题**：UAS127010205606105134的判责报告中，LLM计算错误：
- 实际：29元 / 29.72元 = 97.58%
- LLM判断："占实付97.6%<10%"（错误）

**修改位置**：行576-582的示例

**修改后**（增加反例）：
```jinja
**示例**：
```
步骤5归一化结果：{platform: 20, merchant: 80, logistics: 0, agent: 0}

正例：
- 诉求赔付金额：3元，实付金额：47.13元，占比：6.4%
- P0-4检查：3 ≤ 30元 ✓，6.4% < 10% ✓ → **触发P0-4**

反例1：
- 诉求赔付金额：30元，实付金额：47.13元，占比：63.6%
- P0-4检查：30 ≤ 30元 ✓，但 63.6% > 10% ✗ → **不触发**（占比过高）

反例2：
- 诉求赔付金额：29元，实付金额：29.72元，占比：97.6%
- P0-4检查：29 ≤ 30元 ✓，但 97.6% > 10% ✗ → **不触发**（几乎全额）

反例3：
- 诉求赔付金额：30元，实付金额：300元，占比：10%
- P0-4检查：30 ≤ 30元 ✓，但 10% = 10% ✗ → **不触发**（必须 < 10%，不含等于）
```
```

## 三、测试计划

### 3.1 测试用例

| 用例 | 升级售后类型 | 诉求 | 实付 | merchant | 期望输出 |
|------|------------|------|------|----------|---------|
| 1 | 超时升级售后单 | 5元 | 100元 | 60% | platform=100% |
| 2 | 处理中升级售后单 | 5元 | 100元 | 60% | merchant=100% |
| 3 | 处理中升级售后单 | 5元 | 100元 | 0% | platform=100% |
| 4 | 超时升级售后单 | 29元 | 29.72元 | 90% | 不触发P0-4（97.6%>10%） |
| 5 | 超时升级售后单+P0-1 | 50元 | 100元 | - | platform=80%, merchant=20% |
| 6 | 超时升级售后单+P0-1 | 30元 | 100元 | - | platform=100% |

### 3.2 测试步骤

1. 修改prompt（修改1）
2. 重置UAS127010205606105134状态为"未处理"
3. 运行测试：`BITABLE_WRITE_ENABLED=1 python scripts/main.py auto --limit 1`
4. 验证：
   - P0-4是否正确判断（不应触发，因为97.6%>10%）
   - 如果P0-1触发（需评估条件5），是否输出platform=80%或100%
5. 如果修改P0-1条件5，用其他满足条件的超时单测试

## 四、影响评估

### 4.1 修改1影响（P0-4超时单逻辑）

**影响范围**：
- 超时升级售后单 + 小额触发（诉求≤30元且占比<10%）
- 根据当前数据：8条超时单中，可能有2-3条会触发小额规则（诉求≤30元）

**风险**：
- 低风险：超时单本身就应该由平台承担时效责任
- 业务合理：小额超时申诉（如5元超时13天），平台承担100%符合预期

### 4.2 修改2影响（P0-1条件5放宽）

**方案A（放宽到1.0）**：
- 影响：偏离0.3-1.0的超时单可以触发P0-1
- 风险：中等，需评估偏离0.3-1.0的商家品质情况

**方案B（放宽到2.0）**：
- 影响：偏离0.3-2.0的超时单可以触发P0-1
- 风险：较高，偏离1.0-2.0的商家可能确实有品质问题

**建议**：先实施修改1，观察效果；修改2需要收集更多数据后评估

### 4.3 修改3影响（示例优化）

**影响范围**：
- 提高LLM计算准确率，减少误触发
- 所有P0-4判断场景

**风险**：
- 无风险：只是增加示例，不改变规则逻辑

## 五、实施建议

**阶段1：紧急修复（修改1+修改3）**
1. P0-4增加超时单特殊逻辑
2. P0-4示例优化（增加反例）
3. 测试验证
4. 提交commit

**阶段2：数据收集与评估（修改2）**
1. 收集超时单的商家偏离倍数分布
2. 分析偏离0.3-1.0、1.0-2.0的商家品质情况
3. 评估P0-1条件5放宽的影响
4. 决定是否实施修改2

**版本号**：
- 修改1+修改3：v0.19.0（P0-4超时单修复+示例优化）
- 修改2（如实施）：v0.20.0（P0-1条件5放宽）
